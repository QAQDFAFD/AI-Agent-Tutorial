# 复制为 .env 后填写。除联网 LLM Demo 外，其余示例无需密钥。
OPENAI_API_KEY=

# 2026-07 官方文档推荐从 GPT-5.6 开始；也可以替换成账户可用、支持工具调用的模型。
OPENAI_MODEL=gpt-5.6

# 可选：使用 OpenAI 兼容服务（DeepSeek/Qwen/中转站等）时填写，详见 docs/setup.md。
# OPENAI_BASE_URL=https://api.deepseek.com

# 可选：第 14 章教学 Agent（tutor/）的检索向量来源。
# openai = 调用 Embedding API（默认，质量高）；
# hash   = 本地哈希向量（零成本零网络；DeepSeek 等不提供 embedding 接口的服务商请用它）。
# TUTOR_EMBEDDING_PROVIDER=hash

# 可选：开启 LangSmith 追踪。
LANGSMITH_TRACING=false
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=ai-agent-tutorial

